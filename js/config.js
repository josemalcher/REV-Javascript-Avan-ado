function setConfig() {
    var texts = {
        "title": "Shopping Control!! (js)"
    };
    document.title = texts.title;
    document.getElementById("navTitle").innerHTML = texts.title;
}

setConfig();